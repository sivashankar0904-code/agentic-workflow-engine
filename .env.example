# Copy to .env — docker-compose reads these.

# Postgres connection string used by the orchestrator.
# Local:   postgres://postgres:postgres@localhost:5432/orchestrator?sslmode=disable
# Compose: postgres://postgres:postgres@postgres:5432/orchestrator?sslmode=disable
DATABASE_URL=postgres://postgres:postgres@localhost:5432/orchestrator?sslmode=disable

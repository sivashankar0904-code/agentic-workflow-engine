# Copy to .env — docker-compose reads these.

# MinIO credentials (also used by the orchestrator as AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY).
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# DAG object storage bucket.
S3_BUCKET=dag-config-bucket

services:
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DAG_DIR: /data/dags
    volumes:
      # Persist DAG YAML files on the host.
      - ./dags:/data/dags
